{
  "scripts": {
    "setup": "ln -sf \"$CONDUCTOR_ROOT_PATH/app/.env\" app/.env && { [ -L app/tests/data ] && rm app/tests/data || true; } && mkdir -p \"$CONDUCTOR_ROOT_PATH/app/tests/data\" && (cd app && uv sync)",
    "run": "cd app && uv run uvicorn main:app --reload"
  }
}
